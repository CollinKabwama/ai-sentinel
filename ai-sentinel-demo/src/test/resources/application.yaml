ai:
  sentinel:
    enabled: true
    mode: MONITOR
    # High throttle so multiple test requests in quick succession are allowed
    throttle-requests-per-second: 100

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus,sentinel
