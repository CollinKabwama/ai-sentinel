/**
 * Identity resolution and trust evaluation SPIs used by the decision pipeline.
 * <p>
 * Implementations typically live in the Spring Boot starter (servlet / Spring Security adapters).
 * Core depends only on {@link dev.aisentinel.core.http.HttpRequestView} and model types.
 */
package dev.aisentinel.core.identity.spi;
