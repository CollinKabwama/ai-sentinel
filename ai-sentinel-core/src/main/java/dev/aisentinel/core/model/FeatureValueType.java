package dev.aisentinel.core.model;

/**
 * Stable feature value kinds for schema documentation and compatibility checks.
 */
public enum FeatureValueType {
    DECIMAL,
    INTEGER,
    LONG,
    HASHED_LONG,
    BUCKETED_INTEGER
}
