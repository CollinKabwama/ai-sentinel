/**
 * Candidate scorer/model artifact contract and validation.
 * <p>
 * These types describe immutable declared artifact metadata and validate
 * identity, integrity metadata, feature-schema binding, input requirements,
 * output compatibility, and capability claims before later runtime loading.
 * Descriptor acceptance does not imply runtime availability, model quality,
 * shadow eligibility, or production authority.
 */
package dev.aisentinel.core.scoring.artifact;
