/**
 * Anomaly scorers (statistical Welford, optional Isolation Forest), composite blending, bounded training buffers,
 * model codec types used on the request path and by background retrain/registry install, and the candidate
 * scorer/model artifact contract under {@link dev.aisentinel.core.scoring.artifact}.
 */
package dev.aisentinel.core.scoring;
