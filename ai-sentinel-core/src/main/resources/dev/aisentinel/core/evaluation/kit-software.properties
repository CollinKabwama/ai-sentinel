# Packaging software version emitted as provenance.softwareVersion on Kit results.
# Must match the ai-sentinel parent POM version. This is NOT a build/commit identity
# and must never be written to aiSentinelBuildId.
softwareVersion=0.4.0
