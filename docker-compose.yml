# Optional local Redis for multi-instance quarantine and distributed throttle experiments.
# Not required for unit tests (starter validation tests use Testcontainers when Docker is available).
services:
  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
